name: linter

on: [push]

jobs:
  my_testing:
    runs-on: ubuntu-latest
    steps:
      - name: repository
        uses: actions/checkout@v2
      - name: install dependences
        run: npm install
      - name: linter
        run: make lint
