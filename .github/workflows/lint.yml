name: linter

on: push

jobs:
  my_testing:
    runs-on: ubuntu-latest

    steps:
      - name: repository
        uses: actions/checkout@v2
        uses: actions/setup-node@v1

      - name: make lint
        run: make lint
